<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Setting up log path and log file name -->
    <property name="LOG_PATH" value="/var/log/spring-boot-applications/mcp-human-resources" />
    <property name="LOG_FILE_NAME" value="mcp-human-resources" />
    <property name="CALL_ADVISOR_LOG_FILE_NAME" value="mcp-human-resources-call-advisor" />

    <!-- Setting up logging pattern for console logging -->
    <appender name="Console"
        class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>
                %white(%d{ISO8601}) %highlight(%-5level) [%yellow(%t)] : %logger{36} - %msg%n%throwable
            </pattern>
        </encoder>
    </appender>

    <!-- Setting the filename and logging pattern for log file -->
    <appender name="File"
        class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
        <encoder
            class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level : %logger{36} - %msg%n
            </Pattern>
        </encoder>
      
        <!-- Setting up a rolling policy with rolling done
              daily and when file size is 10MB-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archived/${LOG_FILE_NAME}-%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
        </rollingPolicy>
    </appender>

    <!-- Setting the filename and logging pattern for log file -->
    <appender name="CallAdvisorFile"
        class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${CALL_ADVISOR_LOG_FILE_NAME}.log</file>
        <encoder
            class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level : %logger{36} - %msg%n
            </Pattern>
        </encoder>
      
        <!-- Setting up a rolling policy with rolling done
              daily and when file size is 10MB-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archived/${CALL_ADVISOR_LOG_FILE_NAME}-%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
        </rollingPolicy>
    </appender>

    <root level="INFO">
        <appender-ref ref="Console" />
        <appender-ref ref="File" />
    </root>
    
    <!-- Logging at DEBUG level -->
     <logger name="com.megacorp" level="DEBUG" additivity="false">
        <appender-ref ref="File" />
        <appender-ref ref="Console" />
    </logger>

    <logger name="com.megacorp.humanresources.advisors.ChatClientLoggingAdvisor" level="DEBUG" additivity="false">
        <appender-ref ref="CallAdvisorFile" />
    </logger>

    <logger name="org.springframework.context.support.PostProcessorRegistrationDelegate$BeanPostProcessorChecker" level="ERROR" />
    <logger name="org.springframework.mcp.protocol.tool.SyncMcpToolProvider" level="ERROR" />
    <logger name="org.springframework.mcp.server.protocol.tool.SyncMcpToolProvider" level="ERROR" />
    <logger name="org.springframework.ai.mcp.server.protocol.tool.SyncMcpToolProvider" level="ERROR" />
    <logger name="org.springframework.mcp.protocol.sampling.SyncMcpSamplingProvider" level="ERROR" />
    <logger name="org.springframework.mcp.server.protocol.sampling.SyncMcpSamplingProvider" level="ERROR" />
    <logger name="org.springframework.ai.mcp.server.protocol.sampling.SyncMcpSamplingProvider" level="ERROR" />
    <logger name="org.springframework.mcp.protocol.elicitation.SyncMcpElicitationProvider" level="ERROR" />
    <logger name="org.springframework.mcp.server.protocol.elicitation.SyncMcpElicitationProvider" level="ERROR" />
    <logger name="org.springframework.ai.mcp.server.protocol.elicitation.SyncMcpElicitationProvider" level="ERROR" />
    <logger name="org.springframework.beans.factory.support.DisposableBeanAdapter" level="ERROR" />
    
</configuration>